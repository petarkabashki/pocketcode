---
name: map
description: Transform a list with parallel-map and answer from the mapped results
vm_entry: decide
vm_modules:
  - vm/router
---

Run a pure data fan-out with `parallel-map` and format the collected results into a final answer.