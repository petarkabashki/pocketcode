schema_version: 1
name: stackvm_nested_structured_return_example
description: Normalize tool data, let a VM delegate return nested YAML, and finalize from nested fields in the caller
flows:
  normalize:
    markdown: flows/normalize.md
  confirm_delegate:
    markdown: flows/confirm_delegate.md
