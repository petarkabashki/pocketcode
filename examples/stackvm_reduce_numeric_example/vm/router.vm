[ "score" dict-get? dup none? [ drop 0 ] [ int> ] if ] "item-score" define

[
  "core.read_file"
  [
    "{path: reduce_numeric_payload.yaml}" yaml>
  ]
  [
    last-tool-result failure?
    [ "Numeric reduce failed: missing tool payload file." answer ]
    [
      common.payload-data "payload" store-set
      "payload" store-get "items" dict-get? dup none?
      [ drop "[]" yaml> ]
      [ ]
      if
      [ item-score ]
      parallel-map
      0
      [ + ]
      reduce
      dup "normalized.total" shared!? drop
      str>
      "numeric total: " swap concat answer
    ]
    if
  ]
  tool-once
] "decide" define
